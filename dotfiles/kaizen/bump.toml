[[steps]]
name = "mise"
run = ["mise", "upgrade"]
capture = ["~/.config/mise.lock"]

[[steps]]
name = "nix"
run = ["nix", "flake", "update", "--flake", "~/.config/nix"]
capture = ["~/.config/nix/flake.lock"]
