// This config is in the KDL format: https://kdl.dev
// "/-" comments out the following node.
// Check the wiki for a full description of the configuration:
// https://yalter.github.io/niri/Configuration:-Introduction
// Input device configuration.
// Find the full list of options on the wiki:
// https://yalter.github.io/niri/Configuration:-Input

{{- $down := join "" (index .kaizen.shortcuts "nav.down") -}}
{{- $up := join "" (index .kaizen.shortcuts "nav.up") -}}
{{- $right := join "" (index .kaizen.shortcuts "nav.right") -}}
{{- $overview := "k" -}}
{{- if eq .layout "colemak" }}{{- $overview = "l" -}}{{ end }}

environment {
    QT_PLUGIN_PATH null
    __EGL_VENDOR_LIBRARY_FILENAMES "/usr/share/glvnd/egl_vendor.d/50_mesa.json"
    __GLX_VENDOR_LIBRARY_NAME "mesa"
    LIBGL_DRIVERS_PATH "/usr/lib64/dri"
    GBM_BACKENDS_PATH "/usr/lib64/gbm"
    VDPAU_DRIVER_PATH "/usr/lib64/vdpau"
    VK_ICD_FILENAMES "/usr/share/vulkan/icd.d/asahi_icd.aarch64.json"
    VK_LAYER_PATH "/usr/share/vulkan/explicit_layer.d:/usr/share/vulkan/implicit_layer.d"
}

input {
    keyboard {
        xkb {
            layout "us,ru"
{{- if eq .layout "colemak" }}
            variant "colemak,"
{{- end }}
            options "grp:caps_toggle"
        }
        //feel free to change it to other input toggle options, i like to use caps_lock as  language toggle        }
        track-layout "global"
        numlock
    }
    touchpad {
        // off
        // tap
        // dwt
        dwtp
    }
    mouse {

    }
    trackpoint {
        // off
        natural-scroll
 // accel-speed 0.2
        // accel-profile "flat"
        // scroll-method "on-button-down"
        // scroll-button 273
        // scroll-button-lock
        // middle-emulation
    }
 // Uncomment this to make the mouse warp to the center of newly focused windows.
    // warp-mouse-to-focus

    // Focus windows and outputs automatically when moving the mouse into them.
    // Setting max-scroll-amount="0%" makes it work only on windows already fully on screen.
    // focus-follows-mouse max-scroll-amount="0%"
}
output "eDP-1" {
    mode "3024x1964@60.004"
    scale 2
}
output "HDMI-A-1" {
    mode "3840x2160@60.000"
    scale 2
    position x=1920 y=0  // Positioned to the right of eDP-1
}
workspace "soc"
workspace "web"
workspace "dev"
workspace "deb"
workspace "oth"
// workspace "term"
// Settings that influence how windows are positioned and sized.
// Find more information on the wiki:
// https://yalter.github.io/niri/Configuration:-Layout
layout {
    background-color "00000020"
    // Set gaps around windows in logical pixels.
    gaps 16
    center-focused-column "never"
    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }
    default-column-width {
        proportion 0.5
    }
    focus-ring {
        off
        width 4
        active-color "#7fc8ff"
        inactive-color "#505050"
    }
    border {
        width 2
        active-color "#7b57ff"
        inactive-color "#2e2a3a"
        urgent-color "#9b0000"
 // active-gradient from="#e5989b" to="#ffb4a2" angle=45 relative-to="workspace-view" in="oklch longer hue"

        // inactive-gradient from="#505050" to="#808080" angle=45 relative-to="workspace-view"
    }
    shadow {
        // on
        draw-behind-window true
        softness 30
        spread 10
        offset x=0 y=5
        color "#0007"
    }
    struts {
        // left 64
        // right 64
        top 0
 // MacBook notch compensation // bottom 64
    }
}
layer-rule {
    place-within-backdrop true
}
spawn-at-startup "sh" "-c" "systemctl --user unset-environment QT_PLUGIN_PATH; systemctl --user set-environment __EGL_VENDOR_LIBRARY_FILENAMES=/usr/share/glvnd/egl_vendor.d/50_mesa.json __GLX_VENDOR_LIBRARY_NAME=mesa LIBGL_DRIVERS_PATH=/usr/lib64/dri GBM_BACKENDS_PATH=/usr/lib64/gbm VDPAU_DRIVER_PATH=/usr/lib64/vdpau VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/asahi_icd.aarch64.json VK_LAYER_PATH=/usr/share/vulkan/explicit_layer.d:/usr/share/vulkan/implicit_layer.d; dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=niri __EGL_VENDOR_LIBRARY_FILENAMES __GLX_VENDOR_LIBRARY_NAME LIBGL_DRIVERS_PATH GBM_BACKENDS_PATH VDPAU_DRIVER_PATH VK_ICD_FILENAMES VK_LAYER_PATH XDG_DATA_DIRS"
spawn-at-startup "ydotoold"
// spawn-at-startup "dms" "run"
spawn-at-startup "vicinae" "server"
spawn-sh-at-startup "pkill xremap; xremap --device 'Apple SPI Keyboard' --watch=config,device ~/.config/xremap/config.yml"
spawn-at-startup "sh" "-c" "sleep 1 && /usr/bin/quickshell -c noctalia-shell"
spawn-at-startup "sh" "-c" "sleep 1; exec hyprvoice serve"
spawn-sh-at-startup "$HOME/.config/bin/wallpaper-rotate"
spawn-sh-at-startup "swayidle -w timeout 300 'qs -c noctalia-shell ipc call lockScreen lock' timeout 900 'systemctl suspend' before-sleep 'qs -c noctalia-shell ipc call lockScreen lock'"
spawn-at-startup "sway-audio-idle-inhibit"
spawn-at-startup "wl-clip-persist" "--clipboard" "regular"
spawn-at-startup "wl-paste --type text --watch cliphist store"
spawn-at-startup "wl-paste --type image --watch cliphist store"
hotkey-overlay {
 // Uncomment this line to disable the "Important Hotkeys" pop-up at startup.
    // skip-at-startup

}
prefer-no-csd
screenshot-path "~/Pictures/Screenshots/Screenshot from %Y-%m-%d %H-%M-%S.png"
// The wiki explains how to configure individual animations:
// https://yalter.github.io/niri/Configuration:-Animations
animations {
 // Uncomment to turn off all animations.
    // off

    // Slow down all animations by this factor. Values below 1 speed them up instead.
    // slowdown 3.0

}
window-rule {
    match app-id="^org\\.wezfurlong\\.wezterm$"
    default-column-width {

    }
}
window-rule {
    match app-id="^telegram-desktop$"
    open-on-workspace "soc"
}
window-rule {
    match app-id="^mattermost$"
    open-on-workspace "soc"
}
window-rule {
    match app-id="^discord$"
    open-on-workspace "soc"
}
window-rule {
    match app-id="^brave-browser"
    open-on-workspace "oth"
}
window-rule {
    match app-id="^zen-browser$"
    open-on-workspace "web"
}
window-rule {
    match app-id="^app.zen_browser.zen$"
    open-on-workspace "web"
}
// Open the Firefox picture-in-picture player as floating by default.
window-rule {
    match app-id="firefox$" title="^Picture-in-Picture$"
    open-floating true
}
window-rule {
    match is-active=true
    draw-border-with-background false
    opacity 0.96
}
window-rule {
    match is-active=false
    opacity 0.94
}
// Rounded corners for all windows.
window-rule {
    geometry-corner-radius 6
    clip-to-geometry true
}
binds {
    Mod5+Shift+Slash {
        show-hotkey-overlay
    }
    Alt+Return hotkey-overlay-title="Open a Terminal: wezterm" {
        spawn "flatpak" "run" "org.wezfurlong.wezterm"
    }
    Mod5+Shift+Return hotkey-overlay-title="Open a Terminal: ghostty" {
        spawn "ghostty"
    }
    Alt+Space hotkey-overlay-title="Run an Application: vicinae" {
        spawn "vicinae" "open"
    }
    Super+Alt+L hotkey-overlay-title="Lock the Screen: noctalia" {
        spawn "qs" "-c" "noctalia-shell" "ipc" "call" "lockScreen" "lock"
    }
    XF86AudioRaiseVolume allow-when-locked=true {
        spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1+ -l 1.0"
    }
    XF86AudioLowerVolume allow-when-locked=true {
        spawn-sh "wpctl set-volume @DEFAULT_AUDIO_SINK@ 0.1-"
    }
    XF86AudioMute allow-when-locked=true {
        spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"
    }
    XF86AudioMicMute allow-when-locked=true {
        spawn-sh "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle"
    }
    XF86AudioPlay allow-when-locked=true {
        spawn-sh "playerctl play-pause"
    }
    XF86AudioStop allow-when-locked=true {
        spawn-sh "playerctl stop"
    }
    XF86AudioPrev allow-when-locked=true {
        spawn-sh "playerctl previous"
    }
    XF86AudioNext allow-when-locked=true {
        spawn-sh "playerctl next"
    }
    XF86MonBrightnessUp allow-when-locked=true {
        spawn "brightnessctl" "--class=backlight" "set" "+5%"
    }
    XF86MonBrightnessDown allow-when-locked=true {
        spawn "brightnessctl" "--class=backlight" "set" "5%-"
    }
    Mod5+Shift+G {
        maximize-window-to-edges
    }
    Mod5+Shift+{{ $overview }} repeat=false {
        toggle-overview
    }
    Mod5+Shift+Q repeat=false {
        close-window
    }
    Mod5+Shift+H {
        focus-column-left
    }
    Mod5+Shift+{{ $right }} {
        focus-column-right
    }
    Mod5+Shift+Super+Left {
        move-column-left
    }
    Mod5+Shift+Super+Down {
        move-window-down
    }
    Mod5+Shift+Super+Up {
        move-window-up
    }
    Mod5+Shift+Super+Right {
        move-column-right
    }
    Mod5+Shift+Super+H {
        move-column-left
    }
    Mod5+Shift+Super+{{ $right }} {
        move-column-right
    }
    Mod5+Shift+Home {
        focus-column-first
    }
    Mod5+Shift+End {
        focus-column-last
    }
    Mod5+Shift+Ctrl+Home {
        move-column-to-first
    }
    Mod5+Shift+Ctrl+End {
        move-column-to-last
    }
    Mod5+Shift+Ctrl+Super+Left {
        move-column-to-monitor-left
    }
    Mod5+Shift+Ctrl+Super+Down {
        move-column-to-monitor-down
    }
    Mod5+Shift+Ctrl+Super+Up {
        move-column-to-monitor-up
    }
    Mod5+Shift+Ctrl+Super+Right {
        move-column-to-monitor-right
    }
    Mod5+Shift+Ctrl+Super+H {
        move-column-to-monitor-left
    }
    Mod5+Shift+Ctrl+Super+{{ $down }} {
        move-column-to-monitor-down
    }
    Mod5+Shift+Ctrl+Super+{{ $up }} {
        move-column-to-monitor-up
    }
    Mod5+Shift+Ctrl+Super+{{ $right }} {
        move-column-to-monitor-right
    }
    Mod5+Shift+Page_Down {
        focus-workspace-down
    }
    Mod5+Shift+Page_Up {
        focus-workspace-up
    }
    Mod5+Shift+{{ $down }} {
        focus-workspace-down
    }
    Mod5+Shift+{{ $up }} {
        focus-workspace-up
    }
    Mod5+Shift+Ctrl+Page_Down {
        move-column-to-workspace-down
    }
    Mod5+Shift+Ctrl+Page_Up {
        move-column-to-workspace-up
    }
    Mod5+Shift+Super+{{ $down }} {
        move-column-to-workspace-down
    }
    Mod5+Shift+Super+{{ $up }} {
        move-column-to-workspace-up
    }
    Mod5+Shift+Super+Page_Down {
        move-workspace-down
    }
    Mod5+Shift+Super+Page_Up {
        move-workspace-up
    }
{{- if ne .layout "colemak" }}
    Mod5+Shift+Super+U {
        move-workspace-down
    }
    Mod5+Shift+Super+I {
        move-workspace-up
    }
{{- end }}
    Mod5+Shift+WheelScrollDown cooldown-ms=150 {
        focus-workspace-down
    }
    Mod5+Shift+WheelScrollUp cooldown-ms=150 {
        focus-workspace-up
    }
    Mod5+Shift+Ctrl+WheelScrollDown cooldown-ms=150 {
        move-column-to-workspace-down
    }
    Mod5+Shift+Ctrl+WheelScrollUp cooldown-ms=150 {
        move-column-to-workspace-up
    }
    Mod5+Shift+WheelScrollRight {
        focus-column-right
    }
    Mod5+Shift+WheelScrollLeft {
        focus-column-left
    }
    Mod5+Shift+Ctrl+WheelScrollRight {
        move-column-right
    }
    Mod5+Shift+Ctrl+WheelScrollLeft {
        move-column-left
    }
    Mod5+Shift+Super+WheelScrollDown {
        focus-column-right
    }
    Mod5+Shift+Super+WheelScrollUp {
        focus-column-left
    }
    Mod5+Shift+Ctrl+Super+WheelScrollDown {
        move-column-right
    }
    Mod5+Shift+Ctrl+Super+WheelScrollUp {
        move-column-left
    }
    Mod5+Shift+1 {
        focus-workspace "soc"
    }
    Mod5+Shift+2 {
        focus-workspace "web"
    }
    Mod5+Shift+3 {
        focus-workspace "dev"
    }
    Mod5+Shift+4 {
        focus-workspace "deb"
    }
    Mod5+Shift+5 {
        focus-workspace "oth"
    }
    Mod5+Shift+O {
        focus-workspace "oth"
    }
    Mod5+Shift+S {
        focus-workspace "soc"
    }
    Mod5+Shift+P {
        focus-workspace "deb"
    }
    Mod5+Shift+D {
        spawn-sh "niri msg action focus-workspace dev && niri msg action focus-column-last"
    }
    Mod5+Shift+Ctrl+D {
        spawn-sh "niri msg action focus-workspace dev && niri msg action focus-column-first"
    }
    Mod5+Shift+W {
        focus-workspace "web"
    }
    Mod5+Shift+Super+1 {
        move-window-to-workspace "soc"
    }
    Mod5+Shift+Super+2 {
        move-window-to-workspace "web"
    }
    Mod5+Shift+Super+3 {
        move-window-to-workspace "dev"
    }
    Mod5+Shift+Super+4 {
        move-window-to-workspace "deb"
    }
    Mod5+Shift+Super+5 {
        move-window-to-workspace "oth"
    }
    Mod5+Shift+Super+O {
        move-window-to-workspace "oth"
    }
    Mod5+Shift+Super+S {
        move-window-to-workspace "soc"
    }
    Mod5+Shift+Super+W {
        move-window-to-workspace "web"
    }
    Mod5+Shift+Super+D {
        move-window-to-workspace "dev"
    }
    Mod5+Shift+Super+P {
        move-window-to-workspace "deb"
    }
    Mod5+Shift+Ctrl+1 {
        move-column-to-workspace "soc"
    }
    Mod5+Shift+Ctrl+2 {
        move-column-to-workspace "dev"
    }
    Mod5+Shift+Ctrl+3 {
        move-column-to-workspace "web"
    }
    Mod5+Shift+Ctrl+4 {
        move-column-to-workspace "deb"
    }
    Mod5+Shift+Ctrl+5 {
        move-column-to-workspace "oth"
    }
    Mod5+Shift+BracketLeft {
        consume-or-expel-window-left
    }
    Mod5+Shift+BracketRight {
        consume-or-expel-window-right
    }
    Mod5+Shift+Comma {
        consume-window-into-column
    }
    Mod5+Shift+Period {
        expel-window-from-column
    }
    Mod5+Shift+R {
        switch-preset-column-width
    }
    Mod5+Shift+Super+R {
        switch-preset-window-height
    }
    Mod5+Shift+Ctrl+R {
        reset-window-height
    }
    Mod5+Shift+F {
        maximize-column
    }
    Mod5+Shift+Super+F {
        fullscreen-window
    }
    Mod5+Shift+Ctrl+F {
        expand-column-to-available-width
    }
    Mod5+Shift+C {
        center-column
    }
    Mod5+Shift+Ctrl+C {
        center-visible-columns
    }
    Mod5+Shift+Minus {
        set-column-width "-10%"
    }
    Mod5+Shift+Equal {
        set-column-width "+10%"
    }
    Mod5+Shift+Super+Minus {
        set-window-height "-10%"
    }
    Mod5+Shift+Super+Equal {
        set-window-height "+10%"
    }
    Mod5+Shift+Super+C {
        toggle-window-floating
    }
    Mod5+Shift+Super+V {
        switch-focus-between-floating-and-tiling
    }    // Actions to switch layouts.
    // Note: if you uncomment these, make sure you do NOT have
    // a matching layout switch hotkey configured in xkb options above.
    // Having both at once on the same hotkey will break the switching,
    // since it will switch twice upon pressing the hotkey (once by xkb, once by niri).
    // Alt+Dot       { switch-layout "next"; }
    // Mod+Shift+Space { switch-layout "prev"; }
    Ctrl+Shift+X {
        spawn-sh "GEOM=$(slurp); canberra-gtk-play -i camera-shutter & grim -g \"$GEOM\" - | gradia"
    }
    Ctrl+Shift+1 {
        spawn-sh "canberra-gtk-play -i camera-shutter & grim - | gradia"
    }
    Ctrl+Shift+2 {
        spawn-sh "canberra-gtk-play -i camera-shutter & grim -g \"$(slurp)\" - | gradia"
    }
    Super+Shift+X {
        spawn-sh "GEOM=$(slurp); canberra-gtk-play -i camera-shutter & grim -g \"$GEOM\" - | satty -f - "
    }
    Ctrl+Super+1 {
        spawn-sh "canberra-gtk-play -i camera-shutter & grim - | satty -f - "
    }
    Ctrl+Super+2 {
        spawn-sh "canberra-gtk-play -i camera-shutter & grim -g \"$(slurp)\" - | satty -f - "
    }
    Alt+Escape allow-inhibiting=false {
        toggle-keyboard-shortcuts-inhibit
    }
    Ctrl+Alt+Shift+Q {
        quit
    }
    Ctrl+Alt+Delete {
        quit
    }
    Ctrl+Super+C {
        spawn "vicinae" "vicinae://launch/clipboard/history?toggle=true"
    }
    Super+Space hotkey-overlay-title="Clipboard History: vicinae" {
        spawn "vicinae" "vicinae://launch/clipboard/history?toggle=true"
    }
    Alt+Shift+E hotkey-overlay-title="Emoji Picker: vicinae" {
        spawn "vicinae" "vicinae://launch/core/search-emojis?toggle=true"
    }
    Super+Shift+Alt+G {
        spawn "vicinae" "vicinae://extensions/rolandleth/kill-process/index"
    }
    Super+Shift+Alt+B {
        spawn "vicinae" "vicinae://extensions/lardissone/raindrop-io/search"
    }
    Super+Shift+G {
        spawn-sh "grim -g \"$(slurp)\" - | tesseract - - -l rus+eng | wl-copy"
    }
    Alt+Tab hotkey-overlay-title="Switch Windows: vicinae" {
        spawn "vicinae" "vicinae://launch/wm/switch-windows?toggle=true"
    }
    Ctrl+Super+3 {
        spawn-sh "dms notify \"Start crop recording\" && wf-recorder --audio -g \"$(slurp)\" -f ~/Videos/crop_$(date +%H%M%S).mp4"
    }
    Ctrl+Super+4 {
        spawn-sh "dms notify \"Start full recording\" && wf-recorder --audio -f ~/Videos/full_$(date +%H%M%S).mp4"
    }
    Ctrl+Super+S {
        spawn-sh "killall -s SIGINT wf-recorder && dms notify \"Stop recording\" && sleep 0.5 && nautilus ~/Videos"
    }
    Ctrl+Backslash {
        spawn-sh "hyprvoice toggle"
    }
    Ctrl+Shift+Backslash {
        // spawn-sh "/home/darkawower/.local/bin/waystt-toggle"
        spawn-sh "pkill -USR2 -n handy"
    }
 // waystt - Speech to Text (clipboard copy)
    // Ctrl+Shift+R {
    //     spawn "/home/darkawower/.local/bin/waystt-toggle"
    // }
 // Ctrl+Backslash {
    //     spawn-sh "hyprvoice toggle && sleep 0.1 && wtype -M ctrl -k v -m ctrl"
    // }
}
include "dms/binds.kdl"
include "dms/cursor.kdl"
